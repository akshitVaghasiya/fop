export enum ItemType {
  LOST = 'LOST',
  FOUND = 'FOUND',
  FREE = 'FREE',
}

export enum ItemStatus {
  ACTIVE = 'ACTIVE',
  CLAIMED = 'CLAIMED',
  COMPLETED = 'COMPLETED',
  RESOLVED = 'RESOLVED',
}
